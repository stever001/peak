"""Peak internal AI operating system — Python tooling layer.

Source assets only. This package holds the SQLAlchemy models and DB tooling for the
controlled engagement database (MySQL). It stores NO data: no client data, no seed
records, no fixtures. Real engagement data lives in the controlled MySQL database and
resolver capsules — never in Git. See docs/DATABASE_SCAFFOLD.md.
"""
