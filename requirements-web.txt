# Phase 201 consultant web application: the thin FastAPI adapter (peak/consultant_api) and
# consultant accounts (peak/accounts). The Next.js frontend lives in web/ and uses npm.
#
# Install with:
#     python3 -m pip install -r requirements-web.txt

-r requirements.txt
fastapi>=0.115
uvicorn>=0.30
argon2-cffi>=23.1
itsdangerous>=2.2
# FastAPI TestClient transport, used by tests/validate_phase201_consultant_web_auth.py.
httpx>=0.27
